class ClassName:
    def __init__(self, attr1, attr2, ...):
        self.attr1 = attr1
        self.attr2 = attr2

    """"
class Car:
    def __init__(self, num_wheels, color):
        self.num_wheels = 4 # hardcoding num_wheels to 4 since all cars have 4 wheels
        self.color = color # making `color` input dynamnic since car color can be diff. colors
    
    """
